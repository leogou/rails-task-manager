<h2>My tasks!</h2>

<ul>
  <% @tasks.each do |task| %>
    <% if task.completed == true %>
      <li><i class="far fa-check-circle"></i><%= link_to task.title, task_path(task) %><i class="fas fa-trash-alt"></i><%= link_to 'Delete', task_path(task), method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% else %>
      <li><i class="far fa-circle"></i><%= link_to task.title, task_path(task) %><i class="fas fa-trash-alt"></i><%= link_to 'Delete', task_path(task), method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% end %>
  <% end %>
</ul>

<div>
  <i class="fas fa-plus"></i><%= link_to 'Add a new task', new_task_path %>
</div>
